# Используем официальный образ n8n
FROM n8nio/n8n:latest

# Экспортируем порт, который использует n8n
EXPOSE 5678

# Не создаем новых пользователей - используем существующего 'node' из базового образа
# USER команда уже установлена в базовом образе n8n

# Запускаем n8n
CMD ["n8n", "start"]
